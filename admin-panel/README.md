# Chatter Admin Panel

Next.js interface for the self-hosted Chatter installer and service manager.
It does not receive the Docker socket. Browser API requests are handled by the
separate `chatter-manager` service.
